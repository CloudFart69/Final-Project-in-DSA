#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("Hello world!\n");
    return 0;
    //for (queuenode *curr = front; curr != NULL; curr = curr->next)
    //{
    //    printf("Value: %d\n", curr->value);
    //}
}
